# ⚠️ IMPORTANTE: NÃO COMPARTILHE NUNCA
# Este arquivo é um TEMPLATE. Crie um .env com seus dados
# O arquivo .env está no .gitignore (não será feito upload)

# Banco de dados (será criado automaticamente)
DATABASE_PATH=livro_caixa.db

# Configuração Streamlit (opcional)
STREAMLIT_SERVER_PORT=8501
STREAMLIT_SERVER_HEADLESS=false
